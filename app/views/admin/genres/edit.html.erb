<!-- ヘッダーの部分テンプレート -->
<%= render partial: 'admin/header_admin' %>
<div class="container">
  <div class='row'>
    <h1>ジャンル編集画面</h1>
  </div>

  <div class='row'>
    <%= form_with model: @genre, url:admin_genre_path, local:true do |f| %>
    <%= f.text_field :name %>
    <%= f.submit '更新' %>
    <% end %>
  </div>
  
</div>

