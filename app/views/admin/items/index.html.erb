<!-- ヘッダーの部分テンプレート -->
<%= render partial: 'admin/header_admin' %>



<div class='container'>
  <div class='row' >
    <h1>商品一覧</h1>
  </div>
    <table class="table">
      <thead>
        <tr>
          <th> 商品ID </th>
          <th> 商品名 </th>
          <th> 税抜価格 </th>
          <th> ジャンル </th>
          <th> ステータス </th>
        </tr>
      </thead>
      <body>
        <% @items.each do |item_info| %>
            <td>
              <%= item_info.name %>
            </td>
            <td>
              <%= item_info.price %>
            </td>
            <td>
              <%= item_info.genre_id %>
            </td>
            <td>
              <%= item_info.is_active %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
</div>
